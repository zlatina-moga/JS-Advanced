function solve (start, end){
    start = Number(start);
    end = Number(end)
    let sum = 0;

    for (let i = start; i <= end; i++){
        sum += i 
    }
    console.log(sum)
}

//solve('1', '5')
solve('-8', '20')